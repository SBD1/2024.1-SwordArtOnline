const { question } = require('../config/readlineConfig');
const { clearTerminal, typeWriter } = require('../config/terminalUtils');

// Criar uma função pra automatizar minhas opções...
const currentOptions = async (options) => {
    
}

module.exports = {
    currentOptions
}